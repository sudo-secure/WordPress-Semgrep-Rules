rules:
  - id: wordpress-random-password-user-input
    languages:
      - php
    severity: ERROR
    message: User-controlled input used in random_password filter
    patterns:
      - pattern: add_filter('random_password', ...)
      - pattern-either:
          - pattern: $_POST
          - pattern: $_GET
          - pattern: $_REQUEST
